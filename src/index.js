const api = require('./Services/Api')
const utils = require('./Services/Utils')
const lsu = require('./Services/LocalStorageUtils')
const auth = require('./Services/AuthServices')

module.exports ={
    api,
    utils,
    lsu,
    auth
}